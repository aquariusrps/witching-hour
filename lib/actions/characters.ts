'use server'

// Character Server Actions
// Implementation added in TWH-2.7 (character approval)
// and TWH-3.x (character creation UI).

// createCharacter()    — user submits new character for approval
// approveCharacter()   — admin approves pending character
// rejectCharacter()    — admin rejects with reason
// updateCharacter()    — user edits their own character
// setActiveCharacter() — user sets their active IC character

export async function characterActionsPlaceholder() {
  // This file is intentionally empty pending TWH-2.7.
  // Do not remove — it signals to the build that this
  // module exists.
}
